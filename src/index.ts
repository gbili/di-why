import DiContainer from './DiContainer';

// Export as both named and default for better ESM/CJS compatibility
export { DiContainer };
export default DiContainer;

// Export instance type for cleaner type imports
export type DiContainerInstance = DiContainer;
